{
  "scripts": {
    "build": "cd reverse-ai-studio && npm install && npm run build"
  },
  "dependencies": {
    "@vercel/node": "^3.2.20"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
