[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "libpq"]

[phases.install]
cmds = ["pip3.11 install -r requirements.txt"]

[start]
cmd = "cd backend && python3.11 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2"
